#Python 2
#Define a function with one int argument that checks to see if the 4th bit from the right is on.

def check_bit4(input):
  mask = 0b1000
  desired = input & mask
  if desired > 0:
    return "on"
  else:
    return "off"
